/**
 * BizBuilt cloud configuration.
 * For private deployments: set apiUrl to your Cloud Run URL (e.g. https://erp.yourcompany.com).
 * Leave apiUrl empty to use browser-only demo mode (localStorage).
 */
window.BIZBUILT_CONFIG = {
  apiUrl: "",
  googleClientId: "",
};
