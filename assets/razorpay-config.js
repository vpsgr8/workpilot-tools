/**
 * Razorpay public config (key_id only — safe for browser).
 *
 * TO COMPLETE SETUP:
 * 1. Deploy server/ to Render (render.yaml) or Cloud Run — see PAYMENTS-SETUP.md
 * 2. Paste your API URL below in apiUrl
 * 3. Push this file to GitHub
 */
window.RAZORPAY_CONFIG = {
  keyId: "rzp_live_T28wwjAyHRA0jd",
  apiUrl: "https://workpilot-payments.onrender.com",
  fallbackMeLink: "https://razorpay.me/@vishalpratapsingh601",
};
